/**
 * Copy and compile each of the files specified in the template
 * manifest into the target directory.
 *
 * @param  {Object} ctx
 * @param  {Callback<Error>} next
 */
export default function (ctx, next) {
  next();
}